# Theme preset (Magic Portfolio / Once UI)
# a = original modern (Geist, cyan, playful UI, dotted background)
# b = classic / lighter (serif headings, sand, conservative, no dots) — default while experimenting
NEXT_PUBLIC_THEME_PRESET=b

# Password for protected pages/routes
PAGE_ACCESS_PASSWORD=password
